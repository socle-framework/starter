name: project_name
description: "Template DDD with chi, postgres, templ, logger"
arch: default
compatible_with: ["socle >= 0.1.0"]
defaults:
  db: postgres
  http: chi
  render: templ
modules:
  - logger
  - auth
  - healthcheck
